---
description: How to start the local preview server
---

# Preview Project

Follow these steps to start the Next.js local development preview server:

1. Start the preview server
// turbo
```bash
npm run dev -- --port 3000 --hostname 0.0.0.0
```

2. Once the server is ready, open a browser and navigate to `http://localhost:3000`.
