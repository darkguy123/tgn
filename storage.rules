rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // TEMPORARY DEBUGGING RULE: Allow anyone to read or write.
    // This is insecure and for diagnostics only.
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
