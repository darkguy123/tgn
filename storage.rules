rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow read and write access for all authenticated users to all paths.
    // This is for debugging purposes and should be secured before production.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
